# PostgreSQL Connection Settings
# These variables are used by docker-compose.yml to set up the database
# and construct the DATABASE_URL for the application.
POSTGRES_PASSWORD=change_me
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# Flask Application Settings
SECRET_KEY=very-secret-key  # openssl rand -hex 32

# The DATABASE_URL is constructed automatically inside docker-compose.yml.
# For local development WITHOUT Docker, you might need to uncomment and use a line like this:
# DATABASE_URL=postgresql://track_finance_user:change_me@localhost:5432/track_finance
